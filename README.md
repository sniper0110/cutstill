# Cutstill

Composition sandbox with a fast look. External agents author Remotion source, render a still, see the frame, patch, repeat.

This commit is a placeholder so the repo is non-empty. First slice lands next.
